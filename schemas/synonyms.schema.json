{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://quizmaster.local/schemas/synonyms.schema.json",
  "title": "en-te-hi-synonyms.json",
  "description": "Headwords with parallel synonym lists. The `synonyms` lists are localizedList values: index i is the same synonym in all three languages, so a generator can show a chosen synonym in every selected language.",
  "type": "object",
  "required": ["schemaVersion", "fileType", "languages", "idPrefix", "generators", "entries"],
  "additionalProperties": false,
  "allOf": [{ "$ref": "common.schema.json#/$defs/envelope" }],
  "properties": {
    "$schema": { "type": "string" },
    "schemaVersion": { "type": "string" },
    "fileType": { "const": "synonyms" },
    "languages": { "type": "array" },
    "updated": { "type": "string" },
    "description": { "type": "string" },
    "idPrefix": { "type": "string", "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$" },
    "generators": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "common.schema.json#/$defs/generatorSpec" }
    },
    "entries": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/entry" }
    }
  },

  "$defs": {
    "entry": {
      "type": "object",
      "required": ["id", "classes", "headword", "synonyms", "tags", "categoryHint"],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string", "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$" },
        "classes": { "$ref": "common.schema.json#/$defs/classList" },
        "headword": { "$ref": "common.schema.json#/$defs/localizedText" },
        "partOfSpeech": { "$ref": "common.schema.json#/$defs/partOfSpeech" },
        "synonyms": {
          "description": "At least two synonyms so a generator can vary the correct option between sessions.",
          "allOf": [{ "$ref": "common.schema.json#/$defs/localizedList" }],
          "properties": {
            "en": { "minItems": 2 },
            "te": { "minItems": 2 },
            "hi": { "minItems": 2 }
          }
        },
        "example": { "$ref": "common.schema.json#/$defs/localizedText" },
        "tags": { "$ref": "common.schema.json#/$defs/tags" },
        "categoryHint": { "$ref": "common.schema.json#/$defs/category" }
      }
    }
  }
}
